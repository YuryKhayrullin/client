# Laravel-Compatible API Skeleton (Optional)

This folder is intentionally lightweight and only provides route shape.

Possible future endpoints:

- `GET /api/health`
- `GET /api/config/default`
- `POST /api/config/validate`
